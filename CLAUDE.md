# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project status

This repository is in its initial state — no source files, build system, or test framework have been set up yet.

## Context

Project name `awesome-browser-ml` suggests in-browser machine learning (e.g. ONNX Runtime Web, TensorFlow.js, WebGPU compute). Update this file once the stack is chosen.

## IDE / tooling

- `.vscode/settings.json` configures Snyk static analysis (org `f5fff108-ce0e-4e20-abbb-24650b8cc4ef`, auto-select org enabled).
